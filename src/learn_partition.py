import os
import sys

import torch
from omegaconf import DictConfig

from src.dataset import SemanticDataset


def learn_partition(cfg: DictConfig):
    pass
    # Load the dataset

    # Create the model

    # Train the model
