# @package _global_
defaults:
  - override /ds: SemanticKITTI
  - override /model: SalsaNext

project: Semantic Segmentation Losses Comparison
train:
  patience: 20
  batch_size: 32
  min_epochs: 100
  loss: CrossEntropyLoss
  learning_rate: 0.01