# @package _global_
defaults:
  - override /ds: KITTI360
  - override /model: SalsaNext

train:
  epochs: 150
  batch_size: 32
  num_workers: 4
  loss: FocalLoss
  learning_rate: 0.01
active:
  strategy: Entropy
  diversity_aware: false
  cloud_partitions: Superpoints

# sbatch --output=Strategy_Entropy.out --error=Strategy_Entropy.out --gpus-per-node=1 scripts/active/full.batch --experiment Strategy_Entropy
