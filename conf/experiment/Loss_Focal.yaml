# @package _global_
defaults:
  - override /ds: SemanticKITTI
  - override /model: SalsaNext

project: Semantic Segmentation Losses Comparison
train:
  patience: 20
  batch_size: 32
  min_epochs: 100
  loss: FocalLoss
  learning_rate: 0.05
  lr_rate_decay: 0.95