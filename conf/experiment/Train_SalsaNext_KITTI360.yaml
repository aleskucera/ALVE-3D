# @package _global_
defaults:
  - override /ds: KITTI360
  - override /model: SalsaNext

project: Semantic Segmentation Models Comparison
train:
  batch_size: 32
  patience: 20
  min_epochs: 100
  loss: CrossEntropyLoss
  learning_rate: 0.05
  lr_decay: 0.95

# sbatch --output=Train_SalsaNext_KITTI360.out --error=Train_SalsaNext_KITTI360.out scripts/train/train_model.batch --experiment Train_SalsaNext_KITTI360