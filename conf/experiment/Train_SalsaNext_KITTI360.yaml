# @package _global_
defaults:
  - override /ds: KITTI360
  - override /model: SalsaNext

project: Semantic Segmentation Models Comparison
train:
  batch_size: 32
  patience: 20
  min_epochs: 100
  loss: CrossEntropyLoss

# sbatch --output=Train_SalsaNext_KITTI360.out --error=Train_SalsaNext_KITTI360.out scripts/train/train_semantic.batch --experiment Train_SalsaNext_KITTI360