# @package _global_
defaults:
  - override /ds: KITTI360
  - override /model: SalsaNext

train:
  epochs: 300
  batch_size: 32
  num_workers: 4
  loss: FocalLoss
  learning_rate: 0.01

# sbatch --output=Baseline_SalsaNext_KITTI360.out --error=Baseline_SalsaNext_KITTI360.out --gpus-per-node=1 scripts/passive/baseline.batch --experiment Baseline_SalsaNext_KITTI360