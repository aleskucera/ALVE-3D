# @package _global_
defaults:
  - override /ds: KITTI360
  - override /model: SalsaNext

train:
  epochs: 200
  batch_size: 32
  num_workers: 4
  loss: LovaszLoss
  learning_rate: 0.01

# sbatch --output=Loss_Lovasz.out --error=Loss_Lovasz.out --gpus-per-node=1 scripts/passive/baseline.batch --experiment Loss_Lovasz