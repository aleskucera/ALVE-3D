# @package _global_
defaults:
  - override /ds: KITTI360
  - override /model: DeepLabV3plus

train:
  epochs: 300
  batch_size: 32
  num_workers: 4
  loss: CrossEntropyLoss
  learning_rate: 0.01

# sbatch --output=Model_DeepLabV3plus.out --error=Model_DeepLabV3plus.out --gpus-per-node=1 scripts/passive/baseline.batch --experiment Model_DeepLabV3plus