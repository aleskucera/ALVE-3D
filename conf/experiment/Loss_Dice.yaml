# @package _global_
defaults:
  - override /ds: SemanticKITTI
  - override /model: SalsaNext

project: Semantic Segmentation Losses Comparison
train:
  patience: 20
  batch_size: 32
  min_epochs: 100
  loss: DiceLoss
  learning_rate: 0.01

# sbatch --output=Loss_Dice.out --error=Loss_Dice.out --gpus-per-node=1 scripts/train/train.batch --experiment Loss_Dice