# @package _global_
defaults:
  - override /ds: SemanticKITTI
  - override /model: SalsaNext

project: Semantic Segmentation Losses Comparison
run_name: SalsaNext - CombinedLoss
train:
  patience: 20
  batch_size: 32
  min_epochs: 100
  loss: CombinedLoss
  learning_rate: 0.01

# sbatch --output=Loss_Combined.out --error=Loss_Combined.out --gpus-per-node=1 scripts/train/train_model.batch --experiment Loss_Combined