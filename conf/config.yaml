defaults:
  - _self_

  # Training and testing settings
  - ds: kitti
  - test: default
  - train: default
  - model: deeplabv3_resnet101

  # Common settings
  - path: default
  - hydra: default

  # Configure launch
  - launch: default

action: none


